# Stacks network: testnet | mainnet (default: testnet)
NEXT_PUBLIC_STACKS_NETWORK=testnet

# Optional: override deployer address so contract IDs use your deployment.
# If unset, frontend uses the deployer in config/addresses.testnet.json (or mainnet).
# Copy the address from the explorer to avoid typos (digit 0 vs letter O).
# Example: NEXT_PUBLIC_STACKS_DEPLOYER_ADDRESS=ST2P3Z4K0MQ0VAB0B4A4JHAQAK0P7Y4R7K1PGGR7Z
# NEXT_PUBLIC_STACKS_DEPLOYER_ADDRESS=

# Convex Database Configuration
# Convex URL for frontend (public) - get from Convex dashboard
NEXT_PUBLIC_CONVEX_URL=
